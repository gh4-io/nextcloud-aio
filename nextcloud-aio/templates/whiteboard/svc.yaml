{{- if .Values.whiteboard.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "nextcloud-aio.whiteboard.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  {{- $versionLabel := dict "app.kubernetes.io/version" ( include "common.images.version" ( dict "imageRoot" .Values.whiteboard.image "chart" .Chart ) ) }}
  {{- $labels := include "common.tplvalues.merge" ( dict "values" ( list .Values.commonLabels $versionLabel ) "context" . ) }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" $labels "context" $ ) | nindent 4 }}
    app.kubernetes.io/part-of: nextcloud-aio-whiteboard
    app.kubernetes.io/component: svc
  {{- if or .Values.whiteboard.service.annotations .Values.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.whiteboard.service.annotations .Values.commonAnnotations ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.whiteboard.service.type }}
  {{- if and .Values.whiteboard.service.clusterIP (eq .Values.whiteboard.service.type "ClusterIP") }}
  clusterIP: {{ .Values.whiteboard.service.clusterIP }}
  {{- end }}
  {{- if .Values.whiteboard.service.sessionAffinity }}
  sessionAffinity: {{ .Values.whiteboard.service.sessionAffinity }}
  {{- end }}
  {{- if .Values.whiteboard.service.sessionAffinityConfig }}
  sessionAffinityConfig: {{- include "common.tplvalues.render" (dict "value" .Values.whiteboard.service.sessionAffinityConfig "context" $) | nindent 4 }}
  {{- end }}
  {{- if or (eq .Values.whiteboard.service.type "LoadBalancer") (eq .Values.whiteboard.service.type "NodePort") }}
  externalTrafficPolicy: {{ .Values.whiteboard.service.externalTrafficPolicy | quote }}
  {{- end }}
  {{- if and (eq .Values.whiteboard.service.type "LoadBalancer") (not (empty .Values.whiteboard.service.loadBalancerSourceRanges)) }}
  loadBalancerSourceRanges: {{ .Values.whiteboard.service.loadBalancerSourceRanges }}
  {{- end }}
  {{- if and (eq .Values.whiteboard.service.type "LoadBalancer") (not (empty .Values.whiteboard.service.loadBalancerIP)) }}
  loadBalancerIP: {{ .Values.whiteboard.service.loadBalancerIP }}
  {{- end }}
  ports:
    - name: http
      port: {{ .Values.whiteboard.service.ports.http }}
      protocol: TCP
      {{- if and (or (eq .Values.whiteboard.service.type "NodePort") (eq .Values.whiteboard.service.type "LoadBalancer")) (not (empty .Values.whiteboard.service.nodePorts.http)) }}
      nodePort: {{ .Values.whiteboard.service.nodePorts.http }}
      targetPort: http
      {{- else if eq .Values.whiteboard.service.type "ClusterIP" }}
      nodePort: null
      targetPort: {{ .Values.whiteboard.containerPorts.http }}
      {{- end }}
    {{- if .Values.whiteboard.service.extraPorts }}
    {{- include "common.tplvalues.render" (dict "value" .Values.whiteboard.service.extraPorts "context" $) | nindent 4 }}
    {{- end }}
  {{- $podLabels := include "common.tplvalues.merge" ( dict "values" ( list .Values.whiteboard.podLabels .Values.commonLabels ) "context" . ) }}
  selector: {{- include "common.labels.matchLabels" ( dict "customLabels" $podLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/part-of: nextcloud-aio-whiteboard
    app.kubernetes.io/component: server
{{- end -}}